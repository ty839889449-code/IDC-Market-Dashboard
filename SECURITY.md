# Security Notes

本包使用 StatiCrypt 兼容的 AES-256-CBC 加密与 HMAC-SHA256 完整性校验，密钥通过 PBKDF2 派生。明文看板不会随部署包发布，答案也没有写入部署文件。

这是适用于纯静态 GitHub Pages 的共享口令保护，不是账号级访问控制：

- 获得答案的用户可以在浏览器中解密并保存内容。
- 公开的密文文件可能被离线猜测，因此答案应避免在仓库、提交记录、Issue 或 README 中公开。
- 修改答案时，应重新加密并覆盖部署文件。
- 不要复用含有明文看板历史的仓库，因为删除当前文件不会清除 Git 历史。
- 如需按人员授权、撤销单个用户或记录访问日志，应改用 Cloudflare Access 等身份访问控制。
